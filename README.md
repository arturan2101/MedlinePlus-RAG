# MedlinePlus-RAG
This project builds a fully local Retrieval-Augmented Generation (RAG) system that answers health-related questions using real data from MedlinePlus. The system requires no paid API calls for inference. All components run inside Google Colab, making it accessible and reproducible.
